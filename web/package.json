{
  "name": "stedt-site",
  "private": true,
  "type": "module",
  "description": "Client-side WASM-SQLite search bundle for the static STEDT site",
  "scripts": {
    "build:search": "mkdir -p ../site/assets && cp node_modules/@sqlite.org/sqlite-wasm/dist/sqlite3.wasm ../site/assets/ && esbuild src/search.js --bundle --format=esm --minify --outfile=../site/assets/stedt-search.js",
    "build:pages": "mkdir -p ../site/assets && esbuild src/reconstructions.js src/search-page.js src/catforms.js src/cite.js src/language-page.js --bundle --format=esm --minify --outdir=../site/assets",
    "build:legacy": "mkdir -p ../site/_legacy/assets && cp node_modules/@sqlite.org/sqlite-wasm/dist/sqlite3.wasm ../site/_legacy/assets/ && esbuild src/legacy-shim.js --bundle --format=esm --minify --outfile=../site/_legacy/assets/legacy-shim.js"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "dependencies": {
    "@sqlite.org/sqlite-wasm": "^3.53.0-build1"
  }
}
